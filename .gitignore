__pycache__/
*.py[cod]

.venv/

.pytest_cache/
.ruff_cache/
.mypy_cache/

dist/
*.egg-info/

.env
.env.*

# commands.log captures full stdout/stderr, including the interactive ED
# login step -- never publish that. The rest of the journal (versions,
# verdict, dcs.log) is the deliverable issue #2 asks for and stays tracked.
spikes/runs/*/commands.log

# ED credential blob -- must never be committed or shipped in a bug report
authdata.bin

# Work trees for issue branches
.worktrees/
